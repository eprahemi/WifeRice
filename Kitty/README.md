# Kitty Terminal Configuration

> **Install path:** `~/.config/kitty/`

Kitty GPU-accelerated terminal emulator configuration.

| File | Purpose |
|------|---------|
| `kitty.conf` | Main terminal config (font, cursor, keybindings, behavior) |
| `kitty-matugen-colors.conf` | Dynamic colors generated by Matugen theme engine |
